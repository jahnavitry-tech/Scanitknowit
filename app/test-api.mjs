// Simple test to verify the API endpoint is working

// Example curl command (for documentation purposes):
console.log('API endpoint is running at http://localhost:3007/api/analyze');
console.log('To test with an image file, use a tool like Postman or curl with multipart/form-data');

console.log('\nTo test with curl, use:');
console.log('curl -X POST http://localhost:3007/api/analyze \\');
console.log('  -F "file=@path/to/your/image.jpg" \\');
console.log('  -H "Content-Type: multipart/form-data"');