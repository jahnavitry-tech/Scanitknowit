{
  "name": "scan-it-know-it-worker",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "typescript": "^5.1.6",
    "wrangler": "^4.44.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler publish --dry-run",
    "publish": "wrangler publish"
  }
}
